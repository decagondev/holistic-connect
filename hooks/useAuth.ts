/**
 * useAuth hook
 * 
 * Simple re-export of useAuth from AuthContext for convenience.
 * This follows the pattern of having hooks in the /hooks directory.
 */

export { useAuth } from '@/contexts/AuthContext';

